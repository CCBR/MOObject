# Read analyses from a directory written by write_multiOmicDataSet_properties

Read analyses from a directory written by
write_multiOmicDataSet_properties

## Usage

``` r
read_analyses_dir_(analyses_dir)
```

## Arguments

- analyses_dir:

  Path to the `analyses/` subdirectory.

## Value

A named list of data frames, lists, or arbitrary R objects.
